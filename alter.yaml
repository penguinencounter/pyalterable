collect:
    rules:
        - 'example/*'

#build:
#    full:
#        use:
#            - image_replace
#        exclude:
#            - '404\.(html|js|ts|css)$'

preprocess:
    use:
        - file_ctx_dbg

plugins:
    example:
        use:
            - parse_html
        path: example/example_plugin.py
        pipeline:
            target: file
            match:
                - ''
            entrypoint: main
