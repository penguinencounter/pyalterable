collect:
    rules:
        - 'example/*'

#build:
#    full:
#        use:
#            - image_replace
#        exclude:
#            - '404\.(html|js|ts|css)$'

preprocess:
    use:
        - example

plugins:
    example:
        use:
            - parse_html
        path: example/example_plugin.py
        pipeline:
            target: file
            match:
                - '.*\.html$'
            entrypoint: main
